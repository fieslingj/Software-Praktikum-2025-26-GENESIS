namespace Genesis.Gameplay.Components;

/// <summary>
/// Marks a collider as a trigger that doesn't block movement
/// but can still detect overlaps.
/// </summary>
public struct TriggerColliderTagComponent { }