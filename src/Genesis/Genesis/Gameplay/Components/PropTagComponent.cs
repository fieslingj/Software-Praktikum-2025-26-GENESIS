namespace Genesis.Gameplay.Components;

/// <summary>
/// Marks a collider as a prop that doesn't block projectiles
/// but can still detect overlaps.
/// </summary>
public struct PropTagComponent { }